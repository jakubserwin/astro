---
import ServiceCardComponent from '@components/ServiceCardComponent.astro'
import { services } from '@mocks'
---
<div class="mt-20 flex flex-col items-center lg:mt-28" >
  <div class="flex flex-col items-center gap-12  lg:flex-row 2xl:gap-40">
    {services.map((service) => (
        <ServiceCardComponent service={service} />
    ))}
  </div>
</div>
<span id="projects" />
